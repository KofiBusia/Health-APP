{
  "name": "vitalage-health",
  "version": "1.0.0",
  "description": "VitalAge — Age-intelligent personal health guide",
  "scripts": {
    "start": "serve . -l $PORT",
    "build": "echo 'Static site — no build step needed'"
  },
  "dependencies": {
    "serve": "^14.2.3"
  },
  "engines": {
    "node": ">=18"
  }
}
